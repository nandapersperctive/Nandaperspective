/* Google Analytics 4 (gtag.js) setup. The tracking script itself is loaded
   from googletagmanager.com via a separate <script> tag in each page's
   <head> — this file only exists locally so the config call is same-origin
   and allowed by the site's CSP (script-src 'self', no unsafe-inline). */
window.dataLayer = window.dataLayer || [];
function gtag() { dataLayer.push(arguments); }
gtag("js", new Date());
gtag("config", "G-9H0X49Z8JN");
